# Write a Python program that uses regular expressions (`re` module) to extract all numbers from a string.
import re

# word = input("Enter a string: ")
# numbers = re.findall(r"\d+", word)
# if numbers:
#     print("Numbers found in here:", numbers)
# else:
#     print("No matches found.")

# Write a Python program using regex to extract all words that start with a vowel (a, e, i, o, u).
# text = input("Enter a string: ")
# vowel_words = re.findall(r"\b[aeiou]\w*", text, re.I)
# if vowel_words:
#     print("Words that start with a vowel:", vowel_words)
# else:
#     print("No matches found.")

# Write a program to extract emails like: username@domain.com
email_text = input("Enter a string")
